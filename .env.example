# Copy this file to `.env` (same directory) and fill in the real key.
# `.env` is gitignored; `.env.example` is tracked as a template.
#
# Obtain an API key at https://console.anthropic.com/settings/keys
# (subscription credits do NOT cover API usage — see Anthropic docs).

ANTHROPIC_API_KEY=sk-ant-api03-REPLACE-ME
